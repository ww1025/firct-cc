# 修复要点

## ① 生成卡住 → 已修复
- `st.rerun()` 改成了 `st.stop()`，OCR 识别后不再无限循环
- 第一次点击：如果上传了照片，OCR 识别 → 回填到文本框 → 显示"请核对后再次点击"
- 第二次点击：直接生成分配方案

## ② 文本框初始有人名 → 已修复  
- 进入院系页时自动清空 `st.session_state.ocr_roster`
- 返回首页也清空
- 文本框初始为空，不会再残留上次的 OCR 结果

## ③ 输入框颜色突兀 → 已修复
- text_area / text_input：白底改米白 `#fefcf8`，边框改柔和 `#d4cfc4`
- 文件上传区：背景改为米白，虚线边框改柔和
- 整体更协调，不刺眼

## ④ 响应慢 → 已优化
- `requirements.txt` 去掉 Flask、waitress（不再需要）
- 版本号用 `>=` 替代精确 `==`，让 pip 解析更快
- 主要慢是因为 PyTorch + EasyOCR 首次加载模型（云服务器需 2-3 分钟冷启动，之后就快了）
