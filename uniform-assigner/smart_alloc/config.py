# -*- coding: utf-8 -*-
"""礼服智能分配 —— 默认规则配置。

所有可调参数集中在这里。设置页把修改后的配置写入 store['config']，
覆盖此处的默认值；核心算法只读取 store 里的 config，不读此处的常量。
"""

DEFAULT_CONFIG = {
    "year": "2026-2027 学年",

    # ── 礼服候选尺码规则 ──
    "uniform": {
        "height_tol": 5,       # 身高 ±cm 内视为候选
        "chest_tol": 4,        # 胸围 ±cm 内视为候选
        "neighbor_count": 1,   # 相邻尺码兜底数量（库存紧缺时扩展）
    },

    # ── 马靴候选尺码规则 ──
    "boots": {
        # 脚长(mm) → 推荐鞋码
        "foot_table": [
            [225, 35], [230, 36], [235, 37], [240, 38], [245, 39],
            [250, 40], [255, 41], [260, 42], [265, 43], [270, 43],
            [275, 44], [280, 45], [285, 46],
        ],
        "neighbor_count": 1,   # 相邻鞋码兜底数量
    },

    # ── 试穿等级 → 匹配分值（越低越优先）──
    "fit_scores": {
        "best": 0,
        "wearable": 10,
        "loose": 20,
        "tight": 30,
        "no": 9999,            # 不可穿：从候选集中剔除
    },

    # 仅规则预测、尚未试穿的尺码基础分值（文档建议 40~60 区间）
    "predicted_score": 50,

    # ── 求解目标权重 ──
    "weights": {
        "sharing": 50,         # 每产生一件共用装备的惩罚
        "old_change": 80,      # 老队员非必要换装惩罚
        "predicted": 20,       # 使用“仅预测未试穿”尺码的惩罚
        "class_risk": {        # 班级冲突矩阵风险分值（同班永远硬禁止）
            "high": 80,
            "mid": 30,
            "low": 5,
        },
    },

    # ── 班级冲突矩阵 ──  {(班A, 班B): "high"/"mid"/"low"}；同班无需填写（硬禁止）
    "class_matrix": {},
}
